# Build cycle log — Lever

Each entry = one full jeo cycle (change → build → test → real-operation verify → commit).

| # | Cycle | Key change | Verification |
|---|-------|-----------|--------------|
| 1 | MVP | engine + metrics + CSV + dashboard + `/api/analyze` + 14 tests + docs | build ✓ · 14 tests ✓ · server 200 · API $6,567 impact |
| 2 | Deep research | `docs/03-deep-research-2026.md` — 2026 trends vs. differentiation | build ✓ |
| 3 | Confidence | per-rec `confidence` (spend+conversion signal) in engine/UI + 4 tests | build ✓ · 18 tests ✓ |
| 4 | Firestore adapter | real `FirestoreStorage` (firebase-admin, lazy import, env-gated) | build ✓ |
| 5 | Storage hardening | deterministic tie-break in `InMemoryStorage.listDatasets` (bug found by test) | 24 tests ✓ |
| 6 | Persist API | `POST /api/analyze {persist,name}` → `createStorage().saveDataset` returns `datasetId` | server: `datasetId=ds-1` |
| 7 | Channel breakdown | `summarizeByChannel` (engine `byChannel`) + UI breakdown cards + 2 tests | build ✓ · 26 tests ✓ |
| 8 | What-if simulator | live EngineConfig sliders (targetRoas/scaleTrigger/scaleStep) re-run analyze in UI | build ✓ · 26 tests ✓ |
| 9 | Export | `recommendationsToCsv` (escaped) + UI "Export CSV" download + 3 tests | build ✓ · 29 tests ✓ |
| 10 | Budget-leak rule | new most-urgent PAUSE for high-spend/zero-conversion waste (closed a "hold a burning campaign" gap) + 2 tests | build ✓ · 31 tests ✓ |
| 11 | Input hardening | clamp negatives in CSV + `sanitizeAdRows` untrusted-payload guard in API + 4 tests | build ✓ · 34 tests ✓ |
| 12 | SEO/marketing meta | OpenGraph + Twitter card + keywords + metadataBase in layout | build ✓ |
| 13 | Empty/clear states + a11y | "no data" + "all clear" panels, aria-label on upload, disabled export when empty | build ✓ |
| 14 | Deploy readiness | `.env.example` (Firebase trio), `.nvmrc`, `engines.node>=20` | build ✓ |
| 15 | Datasets API + store singleton | `GET /api/datasets` list; memoized `createStorage` (fixed per-request reset bug) + test | build ✓ · 35 tests ✓ · live list count=2 |
| 16 | Account health score | exec-level 0..100 portfolio health (ROAS vs target + budget discipline) in engine + KPI + 3 tests | build ✓ · 38 tests ✓ |
| 17 | Demo realism | added a budget-leak entity to seed data; top action now PAUSE $2,400, impact $9,567, health 76 | build ✓ · 38 tests ✓ · live verified |
| 18 | Survey + lint gate | `docs/04-survey-differentiation.md` feature inventory + competitive table; eslint clean | lint ✓ (0 errors) |
| 19 | README marketing | refreshed badges (38 tests), feature bullets (leak/health/what-if/export/persist), architecture + impact ($9,567) | docs |
| 20 | jeo team validation + fixes | architect/critic review caught: (1) high-spend money-loser with 1–4 conv falling into KEEP "healthy" → PAUSE dead-zone fix + regression test; (2) silent persist-failure masking → HTTP 502 `{persisted:false,error}`; (3) reallocation dollars double-counted into headline → `buildReallocation` uses freed spend, headline excludes it (seed $9,567→$7,647 + separate $1,920); (4) unknown platform misattributed to "google" → `"other"` tag. README/badges corrected to 41 tests & honest impact | build ✓ · 41 tests ✓ · live: headline $7,647 / realloc $1,920 / health 76 · persist-fail → 502 |
| 21 | RFC-4180 CSV parser | replaced line-split parser with a full tokenizer (`parseRecords`) that respects quoted fields with embedded commas **and newlines** + `""` escapes; added regression test | build ✓ · 42 tests ✓ · live: POST /api/analyze parses + ranks || 22 | Dataset read API | `GET /api/datasets/:id` returns a saved snapshot's rows + a fresh analysis (404 unknown, 502 store error) — completes the persist→reload loop | build ✓ · 42 tests ✓ · live: ds-1 fetch 9 rows/9 recs · 404 ok |
| 23 | Ponytail-ultra cleanup | removed duplicated doc-comment lines flagged in review (`types.ts` rationale, `storage.ts` InMemory) ; scanned all `src/**` for dup comments (clean) | build ✓ · 42 tests ✓ |
| 24 | Period-over-period creative fatigue | new trend signal: optional `AdRow.priorCtr` + `fatigueDeclineRatio` (0.25) — REFRESH_CREATIVE now also fires when CTR drops ≥25% vs the creative's own prior period (caught *even above channel median*); engine takes the larger of the cross-sectional/trend recovery estimates (capped); CSV `prior_ctr` alias ingest + `sanitizeAdRows`; seeded a TikTok trend-decay entity; backward compatible when `priorCtr` absent. README→47 tests/$7,797/health 77 | build ✓ · 47 tests ✓ · lint 0 · live: tt-3 REFRESH $150 "CTR fell 0.024→0.015 (−37.5%)", headline $7,797.2 / health 77 |
| 25 | LTV-weighted revenue | optional `AdRow.ltvPerConversion` (first-party value); new `effectiveRevenue` helper feeds `computeMetrics`, portfolio totals, channel rollup, and account health — so profit decisions reflect true downstream value, not just immediately-attributed revenue. CSV `ltv_per_conversion` alias ingest + `sanitizeAdRows`; seeded a "looks like a loser, wins on LTV" entity; backward compatible when absent. README→52 tests/$8,109/health 80 | build ✓ · 52 tests ✓ · lint 0 · live: g-3 PAUSE→SCALE $312 (ROAS 1.9), headline $8,109.2 / revenue $35,280 / health 80 |
| 26 | jeo team validation round 2 + fixes | two read-only critics (engine + API/UI) reviewed the committed code. Fixed: (1) **breakeven/target conflation** — PAUSE printed `(< breakeven ${targetRoas})` which is a false statement once the Target ROAS slider ≠ 1.0; breakeven is now the fixed `BREAKEVEN_ROAS = 1.0`; (2) **new REVIEW tier** — profitable-but-below-target entities (only when targetRoas > 1.0) are flagged "Review" instead of mislabeled "healthy", making the Target ROAS slider actually drive the feed; (3) **budget-leak confidence** — a high-spend/zero-conversion leak now scores confidence from spend depth (`spendConfidence`) → 1.0, not the 0.4 a conversion-weighted score gave the *most certain* waste; (4) **reallocation diminishing returns** — moved budget is capped at `scaleStep × winner spend` and the unabsorbable remainder is flagged to spread, instead of crediting flat 80% efficiency on a +57% jump; (5) **fatigue baseline** — `channelMedianCtr` excludes zero-impression rows so an undefined CTR can't drag the median down; (6) **fully deterministic order** — added `entityId` final tiebreak; (7) **CSV formula-injection** — `export.esc()` prefixes `= + - @`/tab/CR fields so exports can't execute in Excel/Sheets; (8) **API config + caps** — `POST /api/analyze` accepts a sanitized `config` (REVIEW/what-if reachable by an agent/MCP) and caps body at 5000 rows; (9) **upload UX/a11y** — 5 MB cap, "Parsing…" state, keyboard-focusable file input (sr-only, not `display:none`). Added `vitest.config.ts` (`@` alias) + first API-route tests. README→67 tests/$8,109/health 80; realloc honest $1,008. | tsc --noEmit ✓ · 67 tests ✓ · lint 0 · build ✓ · live: leak PAUSE conf 1.0, realloc $1260→$1008, POST config targetRoas 1.5 → REVIEW "~$300 short of your goal" |
| 27 | Multi-period sustained creative-fatigue | added a third, more-trustworthy fatigue signal alongside the cross-sectional (vs channel median) and single-period (`priorCtr`) ones. Optional `AdRow.ctrHistory` (consecutive prior-period CTRs, oldest→newest); new pure `metrics.sustainedFatigue` measures decline against the entity's *recent peak* with a ≥2-consecutive-drop guard, so genuine multi-period decay is caught while a single-period blip is not over-fired on. REFRESH takes the max of the three recovery estimates; when the series leads it emits a richer rationale ("CTR declined N periods running to X from a P-period peak of Y (−Z%)") and lifts confidence with the losing-run length. CSV `ctr_history` (pipe-delimited cell) ingest + `sanitizeAdRows` array guard; seeded a Taboola sustained-decay entity (`t-3`); fully backward compatible when absent. README→77 tests/$8,269/health 80 | build ✓ · 77 tests ✓ · lint 0 · live: t-3 REFRESH $160 "CTR declined 3 periods running to 0.014 from a 4-period peak of 0.03 (−53.3%)", headline $8,269.2 / revenue $37,200 / health 80 |
| 28 | Channel-level LTV defaults | extended LTV-weighted revenue to the channel level. New `EngineConfig.channelLtv?: Partial<Record<Channel, number>>` — an average first-party value per conversion by channel, used as a fallback when a row has no per-row `ltvPerConversion`. Precedence in `effectiveRevenue`: per-row LTV > channel LTV > attributed revenue, threaded consistently through `computeMetrics`/totals/`summarizeByChannel`/`accountHealth` so a row's ROAS, the portfolio total, the channel breakdown, and health all agree. `/api/analyze` `sanitizeConfig` accepts a `channelLtv` map, keeping only known channels with finite non-negative rates (unknown/negative keys dropped). Fully backward compatible when absent. README→81 tests | build ✓ · 81 tests ✓ · lint 0 · live: default revenue $37,200/health 80; `channelLtv:{google:60}` → revenue $37,830 / roas 1.34 / health 81 / impact $8,420.4; invalid keys `{bogus,meta:-5}` ignored → identical to default |
| 29 | Budget-pacing — capped winners | new optional `AdRow.budget` (the entity's configured spend cap) + `EngineConfig.pacingThreshold` (0.9). A SCALE winner whose `spend ≥ budget × pacingThreshold` is **budget-capped** — proven demand being throttled rather than a speculative bid — so the engine appends a "raise the cap" note (with the pacing %) and lifts SCALE confidence `+0.1` (capped at 1.0), mirroring the sustained-fatigue evidence lift. Projected impact stays the disciplined diminishing-returns figure (the cap never inflates dollars). CSV `budget`/`daily_budget`/`spend_cap`/`cap` alias ingest (currency-stripped, positive-only) + `sanitizeAdRows` guard; `/api/analyze` `sanitizeConfig` accepts `pacingThreshold`; seeded g-1 with a $4,500 cap (93% pacing). Fully backward compatible when `budget` absent. README→89 tests | build ✓ · 89 tests ✓ · lint 0 · live: g-1 SCALE conf 1.0 "Budget-capped: spent $4200 of a $4500 cap (93% pacing)", headline $8,269.2 / revenue $37,200 / health 80 (unchanged — flag adds no dollars); `pacingThreshold:0.95` drops the flag at 91% pacing |