# Build cycle log — Lever

Each entry = one full jeo cycle (change → build → test → real-operation verify → commit).

| # | Cycle | Key change | Verification |
|---|-------|-----------|--------------|
| 1 | MVP | engine + metrics + CSV + dashboard + `/api/analyze` + 14 tests + docs | build ✓ · 14 tests ✓ · server 200 · API $6,567 impact |
| 2 | Deep research | `docs/03-deep-research-2026.md` — 2026 trends vs. differentiation | build ✓ |
| 3 | Confidence | per-rec `confidence` (spend+conversion signal) in engine/UI + 4 tests | build ✓ · 18 tests ✓ |
| 4 | Firestore adapter | real `FirestoreStorage` (firebase-admin, lazy import, env-gated) | build ✓ |
| 5 | Storage hardening | deterministic tie-break in `InMemoryStorage.listDatasets` (bug found by test) | 24 tests ✓ |
| 6 | Persist API | `POST /api/analyze {persist,name}` → `createStorage().saveDataset` returns `datasetId` | server: `datasetId=ds-1` |
| 7 | Channel breakdown | `summarizeByChannel` (engine `byChannel`) + UI breakdown cards + 2 tests | build ✓ · 26 tests ✓ |
| 8 | What-if simulator | live EngineConfig sliders (targetRoas/scaleTrigger/scaleStep) re-run analyze in UI | build ✓ · 26 tests ✓ |
| 9 | Export | `recommendationsToCsv` (escaped) + UI "Export CSV" download + 3 tests | build ✓ · 29 tests ✓ |
| 10 | Budget-leak rule | new most-urgent PAUSE for high-spend/zero-conversion waste (closed a "hold a burning campaign" gap) + 2 tests | build ✓ · 31 tests ✓ |
| 11 | Input hardening | clamp negatives in CSV + `sanitizeAdRows` untrusted-payload guard in API + 4 tests | build ✓ · 34 tests ✓ |
| 12 | SEO/marketing meta | OpenGraph + Twitter card + keywords + metadataBase in layout | build ✓ |
| 13 | Empty/clear states + a11y | "no data" + "all clear" panels, aria-label on upload, disabled export when empty | build ✓ |
| 14 | Deploy readiness | `.env.example` (Firebase trio), `.nvmrc`, `engines.node>=20` | build ✓ |
| 15 | Datasets API + store singleton | `GET /api/datasets` list; memoized `createStorage` (fixed per-request reset bug) + test | build ✓ · 35 tests ✓ · live list count=2 |
| 16 | Account health score | exec-level 0..100 portfolio health (ROAS vs target + budget discipline) in engine + KPI + 3 tests | build ✓ · 38 tests ✓ |
| 17 | Demo realism | added a budget-leak entity to seed data; top action now PAUSE $2,400, impact $9,567, health 76 | build ✓ · 38 tests ✓ · live verified |
| 18 | Survey + lint gate | `docs/04-survey-differentiation.md` feature inventory + competitive table; eslint clean | lint ✓ (0 errors) |
| 19 | README marketing | refreshed badges (38 tests), feature bullets (leak/health/what-if/export/persist), architecture + impact ($9,567) | docs |
| 20 | jeo team validation + fixes | architect/critic review caught: (1) high-spend money-loser with 1–4 conv falling into KEEP "healthy" → PAUSE dead-zone fix + regression test; (2) silent persist-failure masking → HTTP 502 `{persisted:false,error}`; (3) reallocation dollars double-counted into headline → `buildReallocation` uses freed spend, headline excludes it (seed $9,567→$7,647 + separate $1,920); (4) unknown platform misattributed to "google" → `"other"` tag. README/badges corrected to 41 tests & honest impact | build ✓ · 41 tests ✓ · live: headline $7,647 / realloc $1,920 / health 76 · persist-fail → 502 |
| 21 | RFC-4180 CSV parser | replaced line-split parser with a full tokenizer (`parseRecords`) that respects quoted fields with embedded commas **and newlines** + `""` escapes; added regression test | build ✓ · 42 tests ✓ · live: POST /api/analyze parses + ranks || 22 | Dataset read API | `GET /api/datasets/:id` returns a saved snapshot's rows + a fresh analysis (404 unknown, 502 store error) — completes the persist→reload loop | build ✓ · 42 tests ✓ · live: ds-1 fetch 9 rows/9 recs · 404 ok |
| 23 | Ponytail-ultra cleanup | removed duplicated doc-comment lines flagged in review (`types.ts` rationale, `storage.ts` InMemory) ; scanned all `src/**` for dup comments (clean) | build ✓ · 42 tests ✓ |
| 24 | Period-over-period creative fatigue | new trend signal: optional `AdRow.priorCtr` + `fatigueDeclineRatio` (0.25) — REFRESH_CREATIVE now also fires when CTR drops ≥25% vs the creative's own prior period (caught *even above channel median*); engine takes the larger of the cross-sectional/trend recovery estimates (capped); CSV `prior_ctr` alias ingest + `sanitizeAdRows`; seeded a TikTok trend-decay entity; backward compatible when `priorCtr` absent. README→47 tests/$7,797/health 77 | build ✓ · 47 tests ✓ · lint 0 · live: tt-3 REFRESH $150 "CTR fell 0.024→0.015 (−37.5%)", headline $7,797.2 / health 77 |
| 25 | LTV-weighted revenue | optional `AdRow.ltvPerConversion` (first-party value); new `effectiveRevenue` helper feeds `computeMetrics`, portfolio totals, channel rollup, and account health — so profit decisions reflect true downstream value, not just immediately-attributed revenue. CSV `ltv_per_conversion` alias ingest + `sanitizeAdRows`; seeded a "looks like a loser, wins on LTV" entity; backward compatible when absent. README→52 tests/$8,109/health 80 | build ✓ · 52 tests ✓ · lint 0 · live: g-3 PAUSE→SCALE $312 (ROAS 1.9), headline $8,109.2 / revenue $35,280 / health 80 |
| 26 | jeo team validation round 2 + fixes | two read-only critics (engine + API/UI) reviewed the committed code. Fixed: (1) **breakeven/target conflation** — PAUSE printed `(< breakeven ${targetRoas})` which is a false statement once the Target ROAS slider ≠ 1.0; breakeven is now the fixed `BREAKEVEN_ROAS = 1.0`; (2) **new REVIEW tier** — profitable-but-below-target entities (only when targetRoas > 1.0) are flagged "Review" instead of mislabeled "healthy", making the Target ROAS slider actually drive the feed; (3) **budget-leak confidence** — a high-spend/zero-conversion leak now scores confidence from spend depth (`spendConfidence`) → 1.0, not the 0.4 a conversion-weighted score gave the *most certain* waste; (4) **reallocation diminishing returns** — moved budget is capped at `scaleStep × winner spend` and the unabsorbable remainder is flagged to spread, instead of crediting flat 80% efficiency on a +57% jump; (5) **fatigue baseline** — `channelMedianCtr` excludes zero-impression rows so an undefined CTR can't drag the median down; (6) **fully deterministic order** — added `entityId` final tiebreak; (7) **CSV formula-injection** — `export.esc()` prefixes `= + - @`/tab/CR fields so exports can't execute in Excel/Sheets; (8) **API config + caps** — `POST /api/analyze` accepts a sanitized `config` (REVIEW/what-if reachable by an agent/MCP) and caps body at 5000 rows; (9) **upload UX/a11y** — 5 MB cap, "Parsing…" state, keyboard-focusable file input (sr-only, not `display:none`). Added `vitest.config.ts` (`@` alias) + first API-route tests. README→67 tests/$8,109/health 80; realloc honest $1,008. | tsc --noEmit ✓ · 67 tests ✓ · lint 0 · build ✓ · live: leak PAUSE conf 1.0, realloc $1260→$1008, POST config targetRoas 1.5 → REVIEW "~$300 short of your goal" |
| 27 | Multi-period sustained creative-fatigue | added a third, more-trustworthy fatigue signal alongside the cross-sectional (vs channel median) and single-period (`priorCtr`) ones. Optional `AdRow.ctrHistory` (consecutive prior-period CTRs, oldest→newest); new pure `metrics.sustainedFatigue` measures decline against the entity's *recent peak* with a ≥2-consecutive-drop guard, so genuine multi-period decay is caught while a single-period blip is not over-fired on. REFRESH takes the max of the three recovery estimates; when the series leads it emits a richer rationale ("CTR declined N periods running to X from a P-period peak of Y (−Z%)") and lifts confidence with the losing-run length. CSV `ctr_history` (pipe-delimited cell) ingest + `sanitizeAdRows` array guard; seeded a Taboola sustained-decay entity (`t-3`); fully backward compatible when absent. README→77 tests/$8,269/health 80 | build ✓ · 77 tests ✓ · lint 0 · live: t-3 REFRESH $160 "CTR declined 3 periods running to 0.014 from a 4-period peak of 0.03 (−53.3%)", headline $8,269.2 / revenue $37,200 / health 80 |
| 28 | Channel-level LTV defaults | extended LTV-weighted revenue to the channel level. New `EngineConfig.channelLtv?: Partial<Record<Channel, number>>` — an average first-party value per conversion by channel, used as a fallback when a row has no per-row `ltvPerConversion`. Precedence in `effectiveRevenue`: per-row LTV > channel LTV > attributed revenue, threaded consistently through `computeMetrics`/totals/`summarizeByChannel`/`accountHealth` so a row's ROAS, the portfolio total, the channel breakdown, and health all agree. `/api/analyze` `sanitizeConfig` accepts a `channelLtv` map, keeping only known channels with finite non-negative rates (unknown/negative keys dropped). Fully backward compatible when absent. README→81 tests | build ✓ · 81 tests ✓ · lint 0 · live: default revenue $37,200/health 80; `channelLtv:{google:60}` → revenue $37,830 / roas 1.34 / health 81 / impact $8,420.4; invalid keys `{bogus,meta:-5}` ignored → identical to default |
| 29 | Budget-pacing — capped winners | new optional `AdRow.budget` (the entity's configured spend cap) + `EngineConfig.pacingThreshold` (0.9). A SCALE winner whose `spend ≥ budget × pacingThreshold` is **budget-capped** — proven demand being throttled rather than a speculative bid — so the engine appends a "raise the cap" note (with the pacing %) and lifts SCALE confidence `+0.1` (capped at 1.0), mirroring the sustained-fatigue evidence lift. Projected impact stays the disciplined diminishing-returns figure (the cap never inflates dollars). CSV `budget`/`daily_budget`/`spend_cap`/`cap` alias ingest (currency-stripped, positive-only) + `sanitizeAdRows` guard; `/api/analyze` `sanitizeConfig` accepts `pacingThreshold`; seeded g-1 with a $4,500 cap (93% pacing). Fully backward compatible when `budget` absent. README→89 tests | build ✓ · 89 tests ✓ · lint 0 · live: g-1 SCALE conf 1.0 "Budget-capped: spent $4200 of a $4500 cap (93% pacing)", headline $8,269.2 / revenue $37,200 / health 80 (unchanged — flag adds no dollars); `pacingThreshold:0.95` drops the flag at 91% pacing || 30 | Ouroboros seed frozen | `.ouroboros/seeds/seed.yaml` — immutable contract for the real-data loop (goal, constraints, acceptance, verify cmds). Drift is measured against this. | doc |
| 31 | Crypto primitives | `secrets.ts` `encryptSecret/decryptSecret` — AES-256-GCM, scrypt-derived key from `LEVER_SECRET_KEY`, random per-record salt(16)+iv(12)+tag(16) base64 envelope; wrong-key and tampered-ciphertext both throw; short master secret rejected | 7 crypto tests ✓ (round-trip, unique ciphertext, no-plaintext-leak, wrong-key, tamper, truncation, weak-key) |
| 32 | File credential vault | `FileCredentialVault` writes only ciphertext envelopes (mode 0600) → safe at rest; round-trips across instances; a different master key cannot read the file | 3 tests ✓ (persist-across-instances, ciphertext-only-on-disk, wrong-master-rejected) |
| 33 | In-memory vault + selection | `InMemoryCredentialVault` (random per-process key → zero-config demo still seals); `getVault`/`hasVaultSecret`/`vaultPath` select file vs in-memory; memoized | 6 tests ✓ (vault round-trip, list, remove, selection, path override) |
| 34 | Google Ads connector | `channels/google.ts` — GAQL `search`; `normalize` divides `costMicros`/1e6, maps conversions/value/clicks/impr; handles `search` and `searchStream` shapes; bearer + developer-token headers | 5 tests ✓ |
| 35 | Meta connector | `channels/meta.ts` — Insights; extracts purchase conversions/revenue from `actions`/`action_values` (ignores link_click etc.); `act_` endpoint + access_token | 2 tests ✓ |
| 36 | Taboola connector | `channels/taboola.ts` — Backstage campaign-summary; maps `spent`/`conversions_value`/`cpa_actions_num`; bearer auth | 2 tests ✓ |
| 37 | TikTok connector | `channels/tiktok.ts` — integrated report; reads `campaign_name` from metrics, revenue via candidate-key chain, `Access-Token` header + advertiser_id | 2 tests ✓ |
| 38 | Connector registry | `channels/index.ts` — `CONNECTORS`/`allConnectors`/`getConnector` (undefined for `other`) + `freeTierCatalog`; every connector returns `[]` for junk input rather than throwing | 3 tests ✓ |
| 39 | Local file DB | `LocalFileStorage` implements `StorageAdapter` — one JSON doc on disk, real persistence with no cloud; lists newest-first (`createdAt` desc, `seq` tie-break), strips internal `seq` from results | 3 tests ✓ |
| 40 | Storage selection | `createStorage` priority Firebase → `LocalFileStorage` (`LEVER_DB_PATH`) → in-memory; `hasLocalDbConfig` | 2 selection tests ✓ |
| 41 | Sheets row shaping | `sheets.ts` `buildSheetRows` joins ranked recs to source rows → flat `SheetRow`s, `sortNewestFirst` (date desc, impact desc) + `dedupe` by `date|channel|entityId` | 5 tests ✓ |
| 42 | Sheets push client | `buildSyncPayload`/`pushToSheet` (injectable fetch) → POST JSON to the Apps Script web app; throws on missing URL / non-2xx so failures surface | 4 tests ✓ |
| 43 | Apps Script automation | `apps-script/Code.gs` — `doPost` upserts by key, inserts new rows under the header (newest-first), sorts + trims to `RETENTION_ROWS`, token-gated; `installTrigger` schedules daily `dailyMaintenance`; `doGet` health | manual-deploy script + setup docs |
| 44 | Ingest pipeline | `pipeline.ts` `ingestFromConnectors` — pulls only from connectors with vault creds, reports per-channel `{configured,fetched,error}`, isolates a channel error without aborting the rest | 2 tests ✓ |
| 45 | Pipeline orchestration | `runPipeline` — ingest (or caller rows) → `analyze` → persist (skips empty) → Sheets sync (default when webhook set); sync failure captured, never thrown | 4 tests ✓ |
| 46 | Credentials API | `/api/credentials` — GET onboarding catalog + per-channel `configured` (no secret readback); POST seals creds into the vault; DELETE removes; writes gated by `LEVER_ADMIN_TOKEN` (`x-lever-admin`) | 7 route tests ✓ (incl. "never returns secret values") |
| 47 | Ingest API | `/api/ingest` — runs the pipeline for a validated `{start,end}` window (defaults to trailing 30 days on bad/absent range), accepts rows/csv override, returns result + per-channel status + datasetId + sync; never leaks credentials | 3 route tests ✓ |
| 48 | Shared input validation | extracted `configInput.ts` (`sanitizeConfig` + `parseRange`) so `/api/analyze` and `/api/ingest` sanitize identically — removed duplicated config logic from the analyze route | 89→149 suite green |
| 49 | PM roadmap + spec stack | `docs/05-pm-roadmap.md` (personas, JTBD, architecture, free-tier onboarding, metrics, prioritization, guardrails); `.env.example` documents vault/DB/Sheets/admin vars; `.gitignore` excludes `.lever/` + `*.enc.json` | docs |
| 50 | jeo team review + ship | read-only critic/architect pass over the real-data stack; README refreshed (live link, connectors, vault, Sheets, 149 tests); full verify then push | tsc/build ✓ · 149 tests ✓ · lint 0 |
| 51 | Critic fix — ingest admin gate | critic caught `/api/ingest` running the pipeline with no auth; extracted shared `lib/adminAuth.ts` (`isAdminAuthorized`, constant-time, fail-closed in prod), gated both `/api/ingest` and `/api/credentials` on it | 2 new ingest gate tests ✓ (enforces token / fails closed in prod) · 154 tests ✓ |
| 52 | Critic fix — Sheets dedup key | critic caught Apps Script auto-coercing the `date` cell to a Date on read, so upsert keys never matched and dedup silently re-appended; `normalizeKeyCell_` re-formats Date→YYYY-MM-DD in the sheet timezone so incoming/stored keys agree | build ✓ · lint 0 |
| 53 | Hardening — connector request timeouts | added `fetchWithTimeout` (abort-based, 15s default) in `channels/types.ts` and wired all four connectors through it, so a hung platform API can't stall the whole ingest run; clear timeout error on abort | 4 new timeout tests ✓ |
| 54 | Hardening — vault prod fail-loud | `getVault()` now emits a loud `console.warn` in production when `LEVER_SECRET_KEY` is unset (ephemeral in-memory vault → creds don't persist across restarts/instances); demo still works zero-config | 2 new vault-warning tests ✓ |
| 55 | Doc fix — storage selection comment | corrected stale `createStorage()` doc comment that omitted `LocalFileStorage`; now documents the Firestore→LocalFile→InMemory durability order | docs · 160 tests ✓ · lint 0 · build ✓ |
| 56 | Brand identity — generated symbol + product-design polish | established Lever's visual identity. A `god-tibo-imagen`-generated lever-on-fulcrum symbol is registered as `src/app/icon.png` / `apple-icon.png` / `favicon.ico` and featured in a deterministic 1200×630 `opengraph-image.png` / `twitter-image.png` social card (guaranteed-correct vector typography composited over the AI symbol via `rsvg-convert`, reproducible from `docs/brand-raw/build-og.sh`). A hand-authored inline `LeverMark` SVG logomark anchors the header — unequal-arm leverage geometry (pivot at ~⅓: short arm down, long arm lifted), two-tone via `--brand-ink`/`--brand-profit`, `aria-hidden` (wordmark is the accessible name). New brand color tokens in `globals.css` (`--brand-ink/surface/line/profit/leak/fatigue/review`) are consumed by the wordmark, badge, mark, and a `brand-accent-bar` top edge. Committed to a single consistent light identity — removed the broken `prefers-color-scheme:dark` block that would have flipped the canvas dark while leaving the brand invisible. `docs/BRAND.md` freezes the identity seed. jeo-team critic review caught and we fixed: false currentColor claim, balanced-seesaw→true-leverage geometry, token wiring, dark-mode invisibility, and an a11y double-name. | build ✓ · 160 tests ✓ · lint 0 · live: icon/apple/og/twitter/favicon all HTTP 200, `<link rel=icon>` + `og:image 1200×630` in head; header serves `LeverMark` (path `M5 24 L28 9.8`, brand tokens, `aria-hidden`) + `brand-accent-bar`; built CSS has 0 dark-media rules |
| 57 | Brand typography + fintech-grade number legibility | followed Cycle 56's identity with the typographic layer. Fixed a real defect: `globals.css` hard-coded `font-family: Arial` on `body`, which silently overrode the Geist brand font that `next/font` loads in `layout.tsx` (`--font-geist-sans` on `<html>`) — so the loaded brand typeface never reached body text. `body` now resolves `var(--font-geist-sans)` first with a robust system fallback stack (`ui-sans-serif`/`system-ui`/… → Arial → `sans-serif`). Applied Tailwind's built-in `tabular-nums` to every financial figure that recomputes under the what-if sliders (KPI values, projected impact + action count, channel ROAS/spend/profit/entities, reallocation, action-card confidence/impact/metrics row, slider readouts) so digits share a fixed advance width — columns align and values don't jitter sideways while dragging. Reinforces the deterministic, precise product identity. jeo-team critic review: [APPROVE] — confirmed `--font-geist-sans` wiring, sane fallback order, correct `tabular-nums` cascade onto numeric children without harming alphabetic labels, full coverage of recomputing figures, and no orphan `.tnum` (a custom class was added then removed in favor of Tailwind's utility); applied its one optional polish (wrap the action count in `tabular-nums`). | build ✓ · 160 tests ✓ · lint 0 · live: served `body` resolves Geist (Arial only as deep fallback), built CSS emits `tabular-nums`, page renders the figures aligned |
| 58 | Resilience — transient retry | added `fetchWithRetry` (bounded exponential-backoff wrapping `fetchWithTimeout`) that retries 429/500/502/503/504 + thrown network/timeout errors and returns non-retryable 4xx immediately; routed all four connectors through it so a free-tier rate-limit blip no longer fails a whole ingest run | build ✓ · 167 tests ✓ (+6 retry: 429→success, 5xx exhaust w/ backoff, 4xx no-retry, network retry, persistent throw, budget/status set) · lint 0 |
| 59 | Resilience — Sheets push hardened | `pushToSheet` now goes through `fetchWithRetry` (timeout-bounded + retrying) so a hung/blipping Apps Script web app can't stall or silently drop a sync; added `PipelineOptions.sheetsRetry` knob (tests inject a fake sleep to stay fast) | build ✓ · 167 tests ✓ (+1 Sheets 503→success retry) · lint 0 |
| 60 | Security — constant-time GAS token | replaced the short-circuiting `body.token !== expected` check in `apps-script/Code.gs` `doPost` with `safeEqual_` (length-folded, always scans the longer input) so the public web app's token check can't leak the secret's length/prefix via response timing — closes the deferred critic note | reviewed (GAS) · build ✓ · lint 0 |
| 61 | PM/spec — resilience phase + live verify | documented the resilience phase in `docs/05-pm-roadmap.md`; live-verified the real pipeline: admin-gated `POST /api/ingest` ranks real-shaped rows (PAUSE the ROAS-0.45 loser −$1,100, SCALE the ROAS-4 winner +$792) and prod fails closed (401) without `LEVER_ADMIN_TOKEN`; `GET /api/credentials` serves the free-tier catalog for all four channels | build ✓ · 167 tests ✓ · live: ingest ranks · catalog 200 · prod auth fail-closed |
| 62 | A11y + typographic consistency — sliders announce formatted values, unified ROAS multiplier glyph | extended the typography track into accessibility. The what-if range sliders previously exposed only their raw numeric value to assistive tech, so a screen reader announced "1.25" with no unit/context; the slider markup now sets `aria-valuetext={format(value)}` (per the ARIA APG slider pattern for non-numeric values) so AT speaks the human-readable readout ("1.25×", "30%") and an explicit `aria-label={label}` gives each slider a clean accessible name ("Target ROAS"/"Scale trigger"/"Scale step") instead of the label+value text concatenation from the wrapping `<label>`. The aria work surfaced a real typographic defect: the Target ROAS readout used an ASCII letter `x` (`${v.toFixed(2)}x`) while the Scale-trigger readout used the proper multiplication sign `×` — and the same ASCII `x` leaked into the Blended-ROAS KPI, the channel-table ROAS, and the action-card ROAS. Unified every ROAS multiplier across the page to U+00D7 `×` for one consistent, precise glyph. No DOM-test infra in the suite (vitest is node-env, lib-only), so verified by build + lint + live `next start`: served HTML shows all three sliders carry `aria-label` + `aria-valuetext`, and every served ROAS reads `N.NN×` with zero stray ASCII `x`. | build ✓ · 167 tests ✓ · lint 0 · live: 3 sliders expose aria-label+aria-valuetext, all ROAS glyphs render × |
| 63 | Brand-identity refresh + production product-design pass (god-tibo-imagen symbol re-registered, manifest/theme-color, readability + impact hierarchy) | regenerated the lever-on-fulcrum mark with `god-tibo-imagen` (slate-ink bar+fulcrum, single emerald dot at the lifted right tip on pure white; histogram confirms ink #091428 + emerald + white, an ASCII raster preview confirms the diagonal lever/fulcrum/lifted-tip geometry), normalized it (trim → center → pad → flatten-on-white) into `icon-512` / `apple-icon-180` / multi-res `favicon.ico`, re-composited the 1200×630 OG card via `build-og.sh`, and re-registered all of `src/app/{icon,apple-icon,opengraph-image,twitter-image}.png` + `favicon.ico`; the inline header `LeverMark` SVG already shares that geometry/tokens so favicon, header, and share card now read as one mark. Then a jeo-team critic review drove a focused, UI-only production pass: added `src/app/manifest.ts` (`/manifest.webmanifest`, ink `theme_color`, white `background_color`, icon refs) + a `viewport` export in `layout.tsx` with `themeColor: #0f172a` / `colorScheme: light` so mobile browser chrome wears the brand and the app is installable; raised every running-text `text-slate-400` (≈2.8:1 on white, sub-WCAG-AA) to `text-slate-500` (≈4.6:1) for trust-grade legibility while keeping hierarchy; elevated the payoff line into a hero band — an uppercase "Projected profit lift" label over a `text-2xl font-black` emerald figure with grammatically-correct action count — so "the one move that matters" is the loudest thing on the page; and de-"demo"-ed the chrome ("Seeded demo dataset"→"Sample dataset", "Reset demo"→"Reset sample", empty-state copy) so the product doesn't brand itself a demo. Deferred (logged, not guessed): the placeholder `lever.vercel.app` metadataBase awaits a confirmed production domain. | build ✓ · 167 tests ✓ · lint 0 · live: theme-color `#0f172a` meta + `/manifest.webmanifest` valid JSON, `/icon.png`+`/apple-icon.png` 200, hero "Projected profit lift" renders, zero `slate-400` and zero "demo" chrome in served HTML |
| 64 | Prioritized-actions UX — ranked actionable feed + collapsed "Held" group (fix header/feed count mismatch) | The "Prioritized actions" list mapped over ALL recommendations including zero-impact `KEEP` rows, so on a large account the do-this-next moves were buried under a long "hold and monitor" tail — and it contradicted the hero count ("across N actions") which counts only non-KEEP. A jeo-team architect review picked this as the single highest-leverage UI fix. Split the section into (1) a semantic ranked `<ol>` over the `actionable` slice with `#n` 1-based rank badges (aria-hidden so the `<ol>` conveys position once), making the "ranked, with the math" promise explicit, and (2) a native `<details>/<summary>` "Held — no action" disclosure (collapsed by default, count badge, keyboard-operable, focus-visible ring restored after `marker:content-none`, glyph rotates via `group-open:rotate-90`) holding the KEEP rows. Both lists share one new presentational `RecommendationCard({ r, rank? })` (DRY, no drift). REVIEW (impact 0) deliberately stays in the actionable feed per spec — only KEEP is held; emerald `#047857` stays reserved for profit-positive signals, held group is muted slate. UI-only; engine/lib untouched. Reviewed by jeo-team architect (chose + designed it) and critic (post-impl: OKAY, no fixes). | build ✓ · 167 tests ✓ · lint 0 · live: ranked `<ol>` with `#1` badge renders, `<details>` "Held — no action" + count renders with the single KEEP row inside it (out of the main feed), `group-open:rotate-90` present, hero "Projected profit lift" intact |
| 65 | PWA icon completion (192px + maskable) + deploy-correct dynamic `metadataBase` (Vercel-ready) | Closed the two deferred deployment niceties from Cycle 63. (1) Generated the missing PWA icon variants from the registered god-tibo-imagen master (`docs/brand-raw/cy63/icon-512`): `public/icon-192.png` (the de-facto required Android home-screen size) and `public/icon-maskable.png` — a 512² maskable build where the lever mark is scaled to ~80% and centered on a full-bleed white field so its content (symbol bbox 304×200, ~181px diagonal from center) sits safely inside the maskable 80% safe-zone (205px radius) and is never clipped by circle/squircle masks. Wired both into `manifest.ts` alongside the existing 512 "any" + 180 apple icon, with explicit `purpose: "any"` / `purpose: "maskable"`. (2) Replaced the placeholder hardcoded `metadataBase: new URL("https://lever.vercel.app")` — the thing previously *deferred* awaiting a confirmed domain — with `resolveSiteUrl()`, which derives the canonical origin from Vercel system env at build time: `NEXT_PUBLIC_SITE_URL` override → `VERCEL_PROJECT_PRODUCTION_URL` (stable production domain, present on every deploy) → `VERCEL_URL` (preview) → `http://localhost:3000` fallback. This makes OG/Twitter/canonical absolute URLs correct on production AND preview deployments without ever guessing a domain. UI/brand/docs lane only; engine/lib untouched. | build ✓ · 167 tests ✓ · lint 0 · live: `/manifest.webmanifest` lists icon-192 + icon-maskable(purpose maskable), both serve 200 image/png; an env-set build (`VERCEL_PROJECT_PRODUCTION_URL=lever.example.com`) confirmed `og:image`/`twitter:image` emit `https://lever.example.com/...` (vs `http://localhost:3000/...` with no env), proving the dynamic base resolves at build time as Vercel runs it |
| 66 | Connector pagination | Google Ads (`nextPageToken`), Meta Insights (`paging.next` cursor), and TikTok integrated report (`page`/`page_info.total_page`) connectors now walk every page of a reporting window instead of truncating large accounts to page one; a new `MAX_FETCH_PAGES` (20) safety cap bounds worst-case request volume so a malformed/never-ending cursor can't loop forever or exhaust a free-tier quota. Taboola's campaign-summary report is a single non-paginated response (documented, unchanged). `normalize()` on all three now also accepts an array of accumulated pages. Closes the "Connector pagination" roadmap item from `docs/05-pm-roadmap.md` §6. | build ✓ · 171 tests ✓ (was 167; +4 pagination tests: multi-page walk + cap-enforcement per connector) · lint 0 |
| 67 | Scheduled ingest (cron) | New `GET /api/cron/ingest` runs the same `runPipeline` orchestration on a daily 2-day trailing window (overlap so a late-settling conversion is never missed); `vercel.json` registers the Vercel Cron schedule (`0 6 * * *`). Auth via `LEVER_CRON_SECRET` compared against the `Authorization: Bearer` header Vercel Cron sends (constant-time, fails closed in production if unset, mirroring `isAdminAuthorized`). `?days=N` (clamped ≤90) allows a manual backfill trigger. Closes the "Scheduled server-side ingest (cron)" roadmap item. | build ✓ · 176 tests ✓ (+5: default window, days-override clamp/fallback, bearer-token gate, prod fail-closed) · lint 0 |
| 68 | Multi-tenant vault namespacing | New `vaultKey(channel, accountId)` / `DEFAULT_ACCOUNT_ID` / `isValidAccountId` in `src/lib/secrets.ts`: the default account keeps the original unprefixed key (zero-config demo, existing vault files, and tests are unaffected); any other `accountId` is namespaced `${accountId}::${channel}` so two tenants' credentials for the same channel never collide. `POST/GET/DELETE /api/credentials`, `POST /api/ingest`, and `GET /api/cron/ingest` all accept an optional, validated `accountId`; `runPipeline`/`ingestFromConnectors` thread it through to vault reads and the auto-generated dataset name. `GET /api/credentials` scopes its `configured` flags and stored-key listing to the requested account only — never leaking another tenant's presence. Closes the "Per-account multi-tenant vault namespacing" roadmap item. | build ✓ · 181 tests ✓ (+5: cross-tenant isolation, DELETE scoping, invalid-accountId rejection, vault-read scoping, dataset-name folding) · lint 0 |
